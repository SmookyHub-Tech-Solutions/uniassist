# Local secrets — never commit these
.env
config.local.php

# SQLite databases (contain real user data)
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# OS / editor noise
.DS_Store
Thumbs.db
.idea/
.vscode/

# Node (Tailwind build) — rebuild with: ./node_modules/.bin/tailwindcss -i assets/input.css -o assets/tailwind.min.css --minify
node_modules/
